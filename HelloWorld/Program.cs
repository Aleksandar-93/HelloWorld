using System;

namespace StringPratice
{
    class Program
    {
        static void Main(string[] args)
        {
            //Var data type

            var number = 0; // must bee initaliezed 
            //number = "sometext"; Need to be same type
            number = 2;
            // var num3; Implicitly-typed variables must be initialized
            var text = "text";
            var isTrue = false; //boolean
            //number = null; var canot bee null 
        }
    }
}